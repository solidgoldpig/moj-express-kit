{% macro addBlock(m) %}
{{ addBlocks([m])}}
{% endmacro %}

{% macro addBlocks(blockList) %}
{% for tBlock in blockList %}
{% import tBlock as temp with context %}
{% set Block = Object.assign(Block, temp) %}
{{ setGlobal('Block', Block) }}
{% endfor %}
{% endmacro %}